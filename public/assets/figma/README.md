# Figma Asset Folder

Place exported Figma assets here without recreating them.

Rules:
- Keep original SVG exports unchanged.
- Export raster assets at `2x` when possible.
- Name files with screen name and node id.
- Record every exported file in `manifest.json`.
